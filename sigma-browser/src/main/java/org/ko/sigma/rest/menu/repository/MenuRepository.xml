<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.ko.sigma.rest.menu.repository.MenuRepository">

    <resultMap id="BaseMenuResultMap" type="org.ko.sigma.data.entity.Menu">
        <id column="id" property="id" jdbcType="VARCHAR"/>
        <result column="code" property="code" jdbcType="VARCHAR"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="path" property="path" jdbcType="VARCHAR"/>
        <result column="component" property="component" jdbcType="VARCHAR"/>
        <result column="redirect" property="redirect" jdbcType="VARCHAR"/>
        <result column="meta_json" property="metaJson" jdbcType="VARCHAR"/>
        <result column="parent_code" property="parentCode" jdbcType="VARCHAR"/>
        <result column="enable" property="enable" jdbcType="SMALLINT"/>
        <result column="version" property="version" jdbcType="BIGINT"/>
        <result column="create_user" property="createUser" jdbcType="VARCHAR"/>
        <result column="gmt_create" property="gmtCreate" jdbcType="TIMESTAMP"/>
        <result column="modified_user" property="modifiedUser" jdbcType="VARCHAR"/>
        <result column="gmt_modified" property="gmtModified" jdbcType="TIMESTAMP"/>
    </resultMap>

    <resultMap id="ResultMapWithChildMenu"
               type="org.ko.sigma.rest.menu.dto.MenuDTO"
               extends="BaseMenuResultMap">
        <association property="meta" javaType="org.ko.sigma.data.json.MenuMeta">
            <result column="meta_title" property="title" jdbcType="VARCHAR"/>
            <result column="meta_icon" property="icon" jdbcType="VARCHAR"/>
        </association>
        <collection property="children"
                    javaType="java.util.List"
                    ofType="org.ko.sigma.rest.menu.dto.MenuDTO"
                    column="code"
                    select="queryMenuByParentCode"/>
    </resultMap>

    <select id="queryMenuList" resultMap="ResultMapWithChildMenu">
        SELECT
            id,
            code,
            name,
            path,
            component,
            redirect,
            meta_json,
            meta_json ->> '$.title' as meta_title,
            meta_json ->> '$.icon' as meta_icon,
            parent_code,
            enable,
            version,
            create_user,
            gmt_create,
            modified_user,
            gmt_modified
        FROM t_menu
        WHERE enable = 1
            AND parent_code = '#'
    </select>

    <select id="queryMenuByParentCode" resultMap="ResultMapWithChildMenu" parameterType="java.lang.String">
        SELECT
            id,
            code,
            name,
            path,
            component,
            redirect,
            meta_json,
            meta_json ->> '$.title' as meta_title,
            meta_json ->> '$.icon' as meta_icon,
            parent_code,
            enable,
            version,
            create_user,
            gmt_create,
            modified_user,
            gmt_modified
        FROM t_menu
        WHERE enable = 1
            AND parent_code = #{parentCode}
    </select>

    <select id="queryMenuByRoleCode" resultMap="ResultMapWithChildMenu" parameterType="java.lang.String">
        SELECT
            m.id,
            m.code,
            m.name,
            m.path,
            m.component,
            m.redirect,
            m.meta_json,
            m.meta_json ->> '$.title' as meta_title,
            m.meta_json ->> '$.icon' as meta_icon,
            m.parent_code,
            m.enable,
            m.version,
            m.create_user,
            m.gmt_create,
            m.modified_user,
            m.gmt_modified
        FROM t_role_menu rm
        LEFT JOIN t_menu m ON rm.menu_code = m.code AND m.enable = 1
        WHERE rm.enable = 1 AND rm.role_code = #{roleCode}
    </select>

</mapper>