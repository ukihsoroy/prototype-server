server.port: 1337

spring:
  application.name: sigma-generator
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: tiger
    url: jdbc:mysql://127.0.0.1:3306/sigma_server?autoReconnect=true&amp;useUnicode=true&amp;characterEncoding=utf-8&amp;useSSL=false

# 生成工具配置
ko.generator:
  moduleName: sigma-mysql
  tables: t_user
  java:
    rootPackage: org.ko.sigma.data
    mybatis:
      domain: entity
      mapper: dao
      xml: mappers
  view:
    path: d://
    js: js
    html: module
  generator:
    project:
      enable: false
      name: ko
      path: d://

